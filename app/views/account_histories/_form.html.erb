<%= form_for([@account,@account_history]) do |f| %>
  <% if @account_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_history.errors.count, "error") %> prohibited this account_history from being saved:</h2>
      <ul>
      <% @account_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :credit_rating %><br />
    <%= f.number_field :credit_rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= @account_history.inspect %>